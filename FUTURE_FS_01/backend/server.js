const express=require('express'),cors=require('cors');require('dotenv').config();const app=express(),port=process.env.PORT||5000;app.use(cors());app.use(express.json());app.get('/',(q,s)=>s.json({message:'Portfolio API is running.'}));app.get('/api/health',(q,s)=>s.json({status:'OK'}));app.use('/api/contact',require('./routes/contact'));app.use('/api/projects',require('./routes/projects'));app.listen(port,()=>console.log('Server running on http://localhost:'+port));