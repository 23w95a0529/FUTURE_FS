const menu=document.getElementById('menu'),links=document.getElementById('links');menu.onclick=()=>links.classList.toggle('open');document.querySelectorAll('#links a').forEach(a=>a.onclick=()=>links.classList.remove('open'));
const theme=document.getElementById('theme');if(localStorage.theme==='dark'){document.body.classList.add('dark');theme.textContent='☀️'}theme.onclick=()=>{document.body.classList.toggle('dark');let d=document.body.classList.contains('dark');localStorage.theme=d?'dark':'light';theme.textContent=d?'☀️':'🌙'};
document.getElementById('year').textContent=new Date().getFullYear();document.getElementById('form').onsubmit=async e=>{e.preventDefault();let s=document.getElementById('status');s.textContent='Sending...';try{let r=await fetch('http://localhost:5000/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});let x=await r.json();if(!r.ok)throw Error(x.message);s.textContent=x.message;e.target.reset()}catch(err){s.textContent='Start the backend server to enable database submission.'}};